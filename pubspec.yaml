name: reduced_fluttercommand
version: 0.1.0
description: Implementation of the state management API 'reduced' on top of the state management framework 'FlutterCommand'.
homepage: https://github.com/partmaster/reduced_fluttercommand
repository: https://github.com/partmaster/reduced_fluttercommand.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  reduced: ^0.1.0
  flutter_command: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
